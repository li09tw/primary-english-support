# Cloudflare API Gateway 配置
# 這些變數需要在 Vercel 的環境變數中設定

# Cloudflare Worker API Gateway 的 URL
# 部署 Worker 後會得到類似這樣的 URL：
# https://primary-english-api-gateway.your-account.workers.dev
CLOUDFLARE_WORKER_URL=https://your-worker-url.workers.dev

# API 密鑰，用於驗證來自 Vercel 的請求
# 這個值需要在 Cloudflare Workers 的環境變數中設定
CLOUDFLARE_API_SECRET=your-secret-api-key-here

# 其他環境變數
NODE_ENV=production
